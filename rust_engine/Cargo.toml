[package]
name = "opus_chess"
version = "2.0.0"
edition = "2021"
authors = ["AI Assistant"]
description = "OpusChess - UCI Chess Engine written in Rust"

[dependencies]
rand = "0.8"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
