[package]
name = "opus_chess"
version = "2.1.0"
edition = "2021"
authors = ["AI Assistant"]
description = "OpusChess - UCI Chess Engine written in Rust with multithreading"

[dependencies]
rand = "0.8"
num_cpus = "1.16"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
